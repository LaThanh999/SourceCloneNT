import ROUTERS from "../routers";

const { HOME, CART, INFO, PAYMENT } = ROUTERS;

const PAGE_NAMES = {
  [HOME]: "Home",
  [CART]: "Cart",
  [INFO]: "Info",
  [PAYMENT]: "Payment",
};

export default PAGE_NAMES;
